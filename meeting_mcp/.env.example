# Example .env for meeting_mcp
# Path to Google service account JSON file
# Preferred local dev: place credentials at `meeting_mcp/config/credentials.json` and set the path here
MCP_SERVICE_ACCOUNT_FILE=meeting_mcp/config/credentials.json
# Optional: calendar id (email)
MCP_CALENDAR_ID=primary
# API key for protecting endpoints
MCP_API_KEY=replace-with-a-strong-random-key
# Comma-separated allowed origins for CORS
MCP_ALLOWED_ORIGINS=http://localhost,http://127.0.0.1
# Optional OAuth token path if using user consent flow
MCP_OAUTH_TOKEN_PATH=token.pickle
